#include <stdio.h>
#include <string.h>

int main ()
{

	char c [50] ;

	int count=0, i, length ;
	printf ("Enter a string: ");
	fflush(stdin); fflush(stdout);
	scanf ("%[^\n]%*c",&c);



	for (i=0 ; c[i]!='\0' ; i++){

	}
    printf("length of string= %d",i);

}
