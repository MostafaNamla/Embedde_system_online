#include <stdio.h>

int main()
{
    int number ;
    printf("Enter of the number :");
    scanf("%d",&number);
    if(number%2==0)
        printf("the even number %d",number);
    else
        printf("the odd number %d",number);


}
